<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masai.sainath.expensemanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/moneymanagericon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/moneymanagericon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseManager">
        <activity
            android:name=".views.EditTransaction"
            android:exported="true" />
        <activity
            android:name=".views.ShowTransaction"
            android:exported="true" />
        <activity
            android:name=".views.AddActivity"
            android:exported="true" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true" />
        <activity
            android:name=".views.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>